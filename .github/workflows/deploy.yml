name: Build and Deploy to GitHub Pages

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Emscripten
      uses: mymindstorm/setup-emsdk@v14
      with:
        version: '3.1.51'
        actions-cache-folder: 'emsdk-cache'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake ninja-build

    - name: Clone and build raylib
      run: |
        git clone --depth 1 --branch 5.0 https://github.com/raysan5/raylib.git
        cd raylib
        mkdir -p build_web
        cd build_web
        emcmake cmake .. -G Ninja \
          -DPLATFORM=Web \
          -DCMAKE_BUILD_TYPE=Release \
          -DGRAPHICS=GRAPHICS_API_OPENGL_ES2 \
          -DBUILD_EXAMPLES=OFF
        cmake --build . --target raylib

    - name: Configure CMake
      run: |
        mkdir -p web_build
        cd web_build
        emcmake cmake .. -G Ninja \
          -DRAYLIB_PATH="${GITHUB_WORKSPACE}/raylib/build_web/raylib"

    - name: Build
      run: |
        cd web_build
        cmake --build .

    - name: Prepare deployment
      run: |
        mkdir -p public
        cp web_build/helbreath_web.html public/index.html
        cp web_build/helbreath_web.js public/
        cp web_build/helbreath_web.wasm public/
        cp web_build/helbreath_web.data public/

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: 'public'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
